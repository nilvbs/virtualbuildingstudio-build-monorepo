auto-install-peers=true
strict-peer-dependencies=false

# Expo / React Native need flattened resolution for Metro (pnpm nests by default).
node-linker=hoisted
# Windows: avoid flaky hardlink races that produce *_tmp_* ENOENT during install.
package-import-method=copy
