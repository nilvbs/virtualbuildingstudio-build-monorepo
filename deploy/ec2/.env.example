# Copy to the EC2 stage folder as `.env` (same directory as docker-compose.yml).
# chmod 600 .env
#
#   ~/BLD/stage/
#     docker-compose.yml
#     .env
#     bld-api-local.tar   (optional; load into Docker once)

NODE_ENV=production
API_PORT=4000
LOG_LEVEL=info

# Native Postgres on the EC2 host (not in Docker), port 5435.
DATABASE_URL=postgresql://surveylink:surveylink@host.docker.internal:5435/surveylink?schema=public
DIRECT_DATABASE_URL=postgresql://surveylink:surveylink@host.docker.internal:5435/surveylink?schema=public

WEB_APP_URL=https://staging.yourdomain.com
CORS_ORIGINS=https://staging.yourdomain.com

AUTH0_DOMAIN=
AUTH0_AUDIENCE=
AUTH0_CLIENT_ID=
AUTH0_CLIENT_SECRET=
AUTH0_DB_CONNECTION=Username-Password-Authentication
AUTH0_MGMT_CLIENT_ID=
AUTH0_MGMT_CLIENT_SECRET=
AUTH0_ROLES_CLAIM=https://surveylink.app/roles
AUTH0_GOOGLE_CONNECTION=google-oauth2

AWS_REGION=us-east-2
S3_BUCKET=bld-build

SENTRY_DSN=
REDIS_URL=
SUPER_ADMIN_EMAIL=
SUPER_ADMIN_PASSWORD=
SUPER_ADMIN_PHONE=+10000000001
SUPER_ADMIN_NAME=Super Admin

AUTH_DEV_MODE=false
