'use client';

import { ActionToasts } from './action-toasts';

/** Global toast host — works on onboarding/landing as well as AppShell pages. */
export function AppToasts() {
  return <ActionToasts />;
}
