# Deprecated monolithic CI — replaced by path-filtered workflows:
#   ci-packages.yml · ci-api.yml · ci-web.yml · ci-mobile.yml
# Kept disabled so PRs only run the pipelines that match changed paths.

name: CI (legacy — disabled)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use ci-packages / ci-api / ci-web / ci-mobile instead."
